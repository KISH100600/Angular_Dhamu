let userName: (Number | String | Boolean)[] = [1, 2, '34', false];
const password: Number = 23;

let user: any = 'jd'

user = [45];

let boolData = true;

boolData = false;

let mobile: unknown = '';

mobile = 'jd';

let email: never;

let pass: undefined;

let pass1: null = null;

console.log(userName, password, user, boolData, mobile, pass, pass1);